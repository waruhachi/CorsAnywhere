// Configuration for CORS Anywhere Worker

// Regular expressions for whitelisted and blacklisted URLs and origins
const whitelistUrls = [
	// Example: "^http.?://www.zibri.org$",
	// "zibri.org$",
	// "test\\..*"
];
const blacklistUrls = [];
const whitelistOrigins = ['.*'];

export { whitelistUrls, blacklistUrls, whitelistOrigins };
